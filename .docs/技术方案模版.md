## 输出要求

- 语言：中文
- 格式：标准 Markdown，可直接粘贴为文档
- 风格：工程严谨，简洁不冗余，时序图 / flowchart 使用 plantuml
- 不要加任何前缀解释，直接输出文档正文

## 文档骨架

严格按以下结构输出，缺失信息用 `TBD` 占位，不要删除任何章节标题：

---

```
# 【功能名称】后端技术方案设计文档

## 前言

本文档为「{功能名称}」的后端方案设计文档，用于指导该功能在 {平台名} 中的开发、测试、部署与评审。

本方案主要解决以下问题：

- {问题1}
- {问题2}
- {问题N}

## 修订历史

| 版本号 | 修订内容 | 修订者 | 日期 |
|--------|----------|--------|------|
| v1.0   | 主体内容定稿 | {作者} | {日期} |

## 词汇表

| 术语 | 含义 |
|------|------|
| {术语} | {含义} |

---

## 1. 项目背景

{现状与痛点描述}

本次需要：

- {要做的事1}
- {要做的事2}

---

## 2. 总体设计

### 2.1 系统边界

本方案涉及的系统与组件：

**业务入口**
- {组件1}

**核心服务**
- {组件2}

**基础设施**
- {组件3}

### 2.2 系统设计概览

{整体流程文字描述}

​```mermaid
flowchart TD
    A[{入口}] --> B[{核心模块}]
    B --> C[{输出}]
​```

### 2.3 时序图

#### {流程名称1}

​```mermaid
sequenceDiagram
    participant A as {角色A}
    participant B as {角色B}
    A->>B: {动作}
    B-->>A: {响应}
​```

#### {流程名称2}

​```mermaid
sequenceDiagram
    ...
​```

---

## 3. MQ 变更设计

| 类型 | Topic | Tag | Consumer Group | 说明 |
|------|-------|-----|----------------|------|
| 新增 | {topic} | {tag} | {cg} | {说明} |

### {Topic名称}

- **消息体结构**：

​```json
{
  "field": "value"
}
​```

- **消费语义**：{at-least-once / exactly-once}
- **幂等策略**：{幂等Key设计}

---

## 4. 方案设计

| 序号 | 逻辑块 | 备注 |
|------|--------|------|
| 1 | {模块名} | {说明} |

### 4.1 模块设计

#### 4.1.1 {模块名}

**职责：**

- {职责1}
- {职责2}

**核心维度：**

- {维度1}
- {维度2}

### 4.2 存储设计

#### 4.2.1 {表名}（{表中文名}）

**设计重点：**

- {重点1}

​```sql
CREATE TABLE `{table_name}` (
    id         bigint unsigned auto_increment,
    created_at bigint unsigned not null comment '创建时间(ms)',
    updated_at bigint unsigned not null comment '更新时间(ms)',
    -- {其他字段}
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='{表注释}';
​```

### 4.3 接口设计

#### 4.3.1 {接口名} `{METHOD} {path}`

**功能：** {描述}

**请求参数：**

​```json
{
  "field": "value"
}
​```

**响应参数：**

​```json
{
  "code": 0,
  "msg": "success",
  "data": {}
}
​```

### 4.4 {核心机制名称，如"消息合并与延迟队列"}

#### Key 设计

| Key | 类型 | 说明 |
|-----|------|------|
| `{key_pattern}` | {Hash/List/ZSET/String} | {说明} |

#### 写入流程

1. {步骤1}
2. {步骤2}

#### 消费/Flush 流程

1. {步骤1}
2. {步骤2}

#### 方案对比（如有多方案）

| 维度 | 方案A | 方案B |
|------|-------|-------|
| 核心思路 | {A} | {B} |
| MQ 开销 | {A} | {B} |
| Redis 开销 | {A} | {B} |
| 实现复杂度 | {A} | {B} |

**选择建议：** {建议}

### 4.N {其他专项模块}

---

## 5. 部署设计

- {模块} 独立部署 / 可水平扩展
- {中间件} 需部署高可用集群

---

## 6. 风险评估

| 风险 | 说明 | 应对 |
|------|------|------|
| {风险} | {说明} | {应对} |

---

## 7. 依赖项

- {依赖项1}：需要 {负责人/团队} 支持，{说明}
- {依赖项2}：TBD

---

## 8. Q&A

**Q: {问题}**
A: {回答}
```

---

## 注意事项
- 表结构必须包含 `id / created_at / updated_at`，时间用 `bigint unsigned` 毫秒
- 接口路径风格保持与项目一致（如 `/api/xxx/yyy`）
- 多方案对比表仅在确实有多种技术选型时输出，否则省略
- 修订历史、词汇表如用户未提供，用占位符，不要编造




