interface:
  display_name: "Coding Repo Investigation"
  short_description: "Trace real code and data paths before design"
  default_prompt: "Use $coding-repo-investigation with feature_dir=.docs/feature-... to trace real repository evidence before design."
policy:
  allow_implicit_invocation: false
